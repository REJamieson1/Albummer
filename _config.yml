# this is only for search engines, it does not appear in the home
# as long as the json variables name and description are provided
title: Albummer
description: rate, share and descover albums
baseurl: ""
url: https://riritheri2reriri.github.io/Albummer/

# this information is shown on the homepage, all fields are optional and
# can be declared either as a single string or as a string array/hash
json:
  name: Albummer
  description:
    - rate, share and descover albums
  cv: assets/cv.pdf
  profiles:
    spotify: https://open.spotify.com/user/31knbdfv7uujcgivlgvv3cljzboa?si=b3e7165279a042c4

exclude:
  - LICENSE
  - README.md

plugins:
  - jekyll-seo-tag
  - jekyll-sitemap

whitelist:
  - jekyll-sitemap

sass:
  sass_dir: _sass
  style: compressed
  sourcemap: never
